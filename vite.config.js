import { defineConfig } from 'vite'

export default defineConfig({
  // Set base to empty string or './' for relative paths, 
  // which works better for GitHub Pages subdirectories.
  base: './',
})
